bind alt-e 'commandline -f repaint; yazi; commandline -f repaint'
bind alt-p 'echo; pull; commandline -f repaint;'
bind ctrl-alt-p 'echo; push; commandline -f repaint;'
bind alt-z __zoxide_fzf_select
bind -M insert alt-z __zoxide_fzf_select
